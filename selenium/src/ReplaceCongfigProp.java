//import org.apache.commons.configuration.ConfigurationException;
//import org.apache.commons.configuration.PropertiesConfiguration;
//public class ReplaceCongfigProp {
//
//public static void main(String[] args) throws ConfigurationException {
        
        // You have to create config.properties file under resources folder or anywhere you want :)
        // Here I'm updating file which is already exist under /Documents
        
        // PropertiesConfiguration: This is the "classic" Properties loader which loads the values from a single or multiple files (which can be chained with "include =".
        // All given path references are either absolute or relative to the file name supplied in the constructor.
        // In this class, empty PropertyConfigurations can be built, properties added and later saved.
        // Include statements are (obviously) not supported if you don't construct a PropertyConfiguration from a file.
        // The properties file syntax is explained here,
        // basically it follows the syntax of the stream parsed by java.util.Properties.load and adds several useful extensions
     //   PropertiesConfiguration config = new PropertiesConfiguration("C:\\Users\\santhosh.kumar\\eclipse-workspace\\selenium\\Config\\TestConfiguration.properties");
        
        // setProperty(): Sets a new value for the specified property.
        // This implementation checks if the auto save mode is enabled and saves the configuration if necessary.
//        config.setProperty("company1", "Crunchify");
//        config.setProperty("company2", "Google");
//        config.setProperty("Crunchify_Address", "NYC, US");
//        config.setProperty("Google_Address", "Mountain View, CA, US");
//        System.out.println(config.getProperty("company1"));
//       
//        // save(): Save the configuration. Before this method can be called a valid file name must have been set.
//        config.save();
//        config.setProperty("company1", "Crunch");
//        System.out.println(config.getProperty("company1"));
//        config.save();
//        System.out.println("Config Property Successfully Updated..");

//}
//}
/**
 * @author Crunchify.com
 * Java Properties Files: How to Update config.properties File in Java?
 */
